# requirements.txt
# All Python dependencies for the Gold Price Predictor v2.
# Streamlit Community Cloud reads this file automatically on deployment.
# Versions are pinned here to avoid compatibility issues — especially
# mlflow which has known conflicts with Python 3.14 and newer protobuf versions.
#
# If running locally: pip install -r requirements.txt

python-dotenv==1.0.1
streamlit==1.32.0
yfinance==0.2.40
scikit-learn==1.4.2
xgboost==2.0.3
mlflow==2.11.3
pandas==2.2.1
numpy==1.26.4
plotly==5.20.0
scipy==1.13.0
vaderSentiment==3.3.2
protobuf==4.25.3
opentelemetry-proto==1.24.0
