# requirements.txt
# All Python dependencies for the Gold Price Predictor v2.
# Streamlit Community Cloud reads this file automatically on deployment
# and installs everything listed here — no manual pip installs needed.
#
# If you're running locally, install everything with:
#   pip install -r requirements.txt

streamlit>=1.32.0
yfinance>=0.2.40
scikit-learn>=1.4.0
xgboost>=2.0.3
mlflow>=2.11.0
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.18.0
scipy>=1.12.0
vaderSentiment>=3.3.2
